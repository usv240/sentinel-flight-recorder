fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
pydantic-settings==2.7.0

# Database
motor==3.6.0
pymongo==4.9.2

# Google Cloud — Gemini 3 via Vertex AI (primary SDK, required for hackathon compliance)
google-genai>=1.16.0

# Google Cloud — BigQuery (Fivetran destination)
google-cloud-bigquery>=3.25.0

# Legacy Gemini SDK (fallback only)
google-generativeai==0.8.3

# HTTP
httpx==0.28.1

# Fivetran MCP subprocess client support
mcp>=1.0.0

# Background scheduler — autonomous monitoring loop (makes SENTINEL truly agentic)
apscheduler>=3.10.4

# Utilities
python-dotenv==1.0.1
python-multipart==0.0.12
aiofiles==24.1.0

# Statistics — real Pearson r computation
numpy==2.1.3
scipy==1.14.1
